<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:helpers="clr-namespace:Land_listing.Resources"
             xmlns:Viewmodel="clr-namespace:Land_listing.ViewModels.Land"
             x:Class="Land_listing.Views.LandView.Lands"
             Title="Land Listing">
    <ContentPage.BindingContext>
        <Viewmodel:LandViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Resources>
        <ResourceDictionary>
            <OnPlatform x:Key="MaterialFontFamily" x:TypeArguments="x:String">
                <On Platform="Android" Value="materialdesignicons-webfont.ttf#Material Design Icons"/>
            </OnPlatform>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid VerticalOptions="FillAndExpand" BackgroundColor="#f8f9fa">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackLayout>
                <StackLayout HorizontalOptions="CenterAndExpand" Margin="0,15">
                    <StackLayout Orientation="Horizontal">
                        <Label HorizontalOptions="CenterAndExpand" Text="Land" FontSize="20" TextColor="#22223b" Margin="68,0"  FontAttributes="Bold" />
                    </StackLayout>

                    <Label Text="Land sites available for viewing" FontSize="15" TextColor="#70798c" FontAttributes="Bold" />
                </StackLayout>
                <StackLayout HorizontalOptions="EndAndExpand" Margin="20,0">
                    <ImageButton Source="{FontImage FontFamily={StaticResource MaterialFontFamily}, Glyph={x:Static helpers:IconFont.Sort},Size=26,Color=#495057}" BackgroundColor="Transparent" />
                </StackLayout>
                <StackLayout>
                    <ListView ItemsSource="{Binding Lands}"                     
                       CachingStrategy="RecycleElement"
                       HasUnevenRows="True" 
                       Grid.ColumnSpan="4"
                       RefreshCommand="{Binding refreshCommand}"
                       RefreshControlColor="Red"
                       SelectionMode="Single"
                       SeparatorVisibility="Default"
                       IsRefreshing="{Binding IsBusy,Mode=OneWay}">

                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <Grid ColumnSpacing="5" Padding="6" Margin="10,0" Grid.ColumnSpan="4" >
                                        <StackLayout >
                                            <Frame CornerRadius="15" HorizontalOptions="FillAndExpand" Padding="5">
                                                <StackLayout Orientation="Vertical">
                                                    <StackLayout Orientation="Horizontal">
                                                        <Label Text=" MWK" TextColor="#f95738" FontAttributes="Bold" FontSize="18"/>
                                                        <Label Text="{Binding Price}" TextColor="#f95738" FontAttributes="Bold" FontSize="18"/>
                                                    </StackLayout>
                                                    <StackLayout Orientation="Horizontal" x:Name="deleteLand" Margin="0,-10">
                                                        <Label HorizontalOptions="StartAndExpand" Margin="5,0">
                                                            <Label.FormattedText>
                                                                <FormattedString>
                                                                    <Span Text="{Binding LandName}" TextColor="#343a40" FontAttributes="Bold" FontSize="17"/>
                                                                    <Span Text=" Site" TextColor="#343a40" FontAttributes="Bold" FontSize="17"/>
                                                                    <Span Text="{x:Static x:Environment.NewLine}" />
                                                                    <Span Text="{Binding Location}" FontSize="16"/>
                                                                </FormattedString>
                                                            </Label.FormattedText>
                                                        </Label>
                                                        <Label Text="{Binding PlotSize}" TextColor="#343a40" Margin="0,5" FontSize="16.5" FontAttributes="Bold"/>
                                                        <StackLayout Orientation="Horizontal" HorizontalOptions="EndAndExpand" Margin="0,0,0,35">
                                                            <StackLayout >
                                                                <ImageButton Margin="0,3" Source="{FontImage FontFamily={StaticResource MaterialFontFamily}, Glyph={x:Static helpers:IconFont.FrequentlyAskedQuestions},Size=26,Color=#343a40}" Clicked="UserRequest_Clicked" CommandParameter="{Binding .}" BackgroundColor="Transparent" />
                                                            </StackLayout>
                                                            <StackLayout>
                                                                <ImageButton Margin="0,3" Source="{FontImage FontFamily={StaticResource MaterialFontFamily}, Glyph={x:Static helpers:IconFont.BookEditOutline},Size=26,Color=#343a40}" Clicked="Delete_Clicked"  BackgroundColor="Transparent" CommandParameter="{Binding .}" />
                                                            </StackLayout>                                                                                                              
                                                        </StackLayout>
                                                    </StackLayout>                                                   
                                                </StackLayout>
                                            </Frame>
                                        </StackLayout>
                                    </Grid>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackLayout>
                <StackLayout Margin="70,0,70,0" >
                    <Button CornerRadius="20" Command="{Binding addlandCommand}" Text=" Add Site"/>
                </StackLayout>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>